package Medium.Medium;

/*

‘Every breath you take, every move you make’ and another word-‘boom’,

for every word keep adding the word index to
 - list of word indexes that has it

start = 0
end = 0
while start < len(sent)
   while(end < len && !foundComplete()) {
        markForword(end)
        end++
   }

    if (end == len && !foundComplete) {
        break;
    }
   len = min(len, end-start)
   remove(start)
   start++
}

markForWord(input, word, index)
    for char in word
        if char in input
            addIndex

foundComplete
    for every char 
 */
public class ShortestStringSW {



}
